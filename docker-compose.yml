version: "3.8"
services:
  api:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - ./data:/app/data
      - ./node_modules:/app/node_modules
    environment:
      - NODE_ENV=development
